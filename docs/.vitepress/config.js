/**
 * VitePress configuration used when building the documentation.
 * The `npm run docs:build` command (documented in `docs/vitepress.md`)
 * relies on this file. Tests in `tests/docs.test.js` execute that command
 * to verify the site generation. Refer to README.md for a list of CLI
 * features that the docs describe.
 */
export default {
  // site-level options
  lang: 'en-US',
  title: 'VitePress',
  description: 'Just playing around.',

  themeConfig: {
    // theme-level options
  }
}